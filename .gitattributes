# 文字檔一律標準化為 LF（跨平台最省事）
* text=auto eol=lf

# Windows 專用腳本可用 CRLF（按需）
*.bat text eol=crlf
*.cmd text eol=crlf

# 保持二進位檔不改行尾
*.png binary
*.jpg binary
*.gif binary
*.pdf binary
*.min.js binary  # 若你不想讓壓縮後的 js 被改行尾，可視需要加這行
